let password = "ilrprojectinprogress";
let result = prompt("Password:");

if (result === password) {
} else {
  alert("Sorry this is a work in progress");
  window.location = "https://www.google.com/";
}
